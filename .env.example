# WordPress Headless CMS GraphQL endpoint
#
# Production / Cloudflare Pages build:
WP_API_URL=https://cms.saasstatshub.com/index.php?graphql

# Local dev with mock fallback (no WP needed):
# WP_API_URL=http://localhost-not-real/graphql
